<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>G1ID | {% block title %}{% endblock %}</title>

        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
          {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
           {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <header class="d-flex flex-row justify-content-between align-items-center">
            <object data="/assets/Logo.svg"></object>
            <nav>
                <ul class="navbar-nav d-flex flex-row">
                    <li><a href="#">Cours</a></li>
                    <li><a href="#">Activité</a></li>
                    <li><a href="#">Exercice</a></li>
                    <li><a href="#">Projets</a></li>
                </ul>
            </nav>
            <div class="d-flex flex-row">
                {% if app.user %}
                    <p>Bonjour {{ app.user.username }},</p>
                {% else %}
                    <p><a href="/login">Connexion</a></p>
                    <p>|</p>
                    <p><a href="/register">Inscription</a></p>
                {% endif %}
            </div>
        </header>
        <main>
            {% block body %}
            {% endblock %}
        </main>
        <footer>
            <p>Réalisé par <a href="mailto:hugo.ben80000@gmail.com">Hugo Benabdelhak</a></p>
            <button type="button" class="btn btn-primary">Left</button>
        </footer>
    </body>
</html>
